server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

        - id: food-service
          uri: lb://food-service
          predicates:
            - Path=/foods/**

        - id: restaurant-service
          uri: lb://restaurant-service
          predicates:
            - Path=/restaurants/**