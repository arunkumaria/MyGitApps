server:
  port: 8082

spring:
  application:
    name: restaurant-service

  main:
    web-application-type: servlet

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
