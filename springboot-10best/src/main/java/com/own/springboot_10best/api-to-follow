echo "Done. Project created at: $BASE"
echo "Run with:  cd backend-toolkit && mvn spring-boot:run  (or ./mvnw spring-boot:run if wrapper added)"
2) Run it
bash
Copy
Edit
bash setup-backend-toolkit.sh
cd backend-toolkit
mvn spring-boot:run
3) Test quickly
Get token:

bash
Copy
Edit
curl -s -X POST http://localhost:8080/auth/login \
 -H 'Content-Type: application/json' \
 -d '{"username":"user","password":"password"}'
Use Authorization: Bearer <token> for:

POST /users { "name":"Alice", "email":"alice@example.com" }

GET /products/page?page=0&size=5&sort=price,desc

POST /bank/transfer { "from":"A001", "to":"A002", "amount":100 }

POST /files/upload (multipart form)

GET /h2-console (JDBC URL: jdbc:h2:mem:testdb)


