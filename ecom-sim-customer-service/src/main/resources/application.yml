spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customers
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

kafka:
  bootstrap-servers: localhost:9092
